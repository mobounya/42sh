Hash Functions in hash.c file and forty_two_sh.h file :

print_hash(t_t *root);
find_key_in_hash(t_t *root, char *key)
free_hash(t_t **root);
insert_name(t_t *root, char *key, char *value)
get_new_node(char *key, char *value);


in typedefs.h, strcut for storing hash values : 

typedef struct s_tree
{
	char			*key;
	char			*value;
	int				count;
	struct s_tree	*right;
	struct s_tree	*left;
}				t_t;

in exec_tools.c :

line 69 :
    value = find_key_in_hash(g_hash, cmd);

line 76 and 77 :
    76: if (ft_strchr(full_path, '/'))
    77: g_hash = insert_name(g_hash, cmd, full_path);

side effects of hash builtin :
	-